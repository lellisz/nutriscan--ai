# QA OPUS — PRAXIS Nutrition — Sessão 2026-04-27

## Veredito: APROVADO COM RESSALVAS

---

### 🔴 Críticos (bloquear merge)

1. **hooks/useStreak.ts:30 — Loop `while(true)` sem guard de iteração** — Adicionar `let i = 0; i < 31` pois query já limita 30 dias.
2. **hooks/useStreak.ts:23 — Streak quebra quando usuário não logou ainda hoje** — Se `!isToday`, começar cursor em `yesterday` para preservar streak do dia (anti-shame).
3. **components/ui/AnimatedBar.tsx — Falta `cancelAnimation(progress)` no cleanup** — Animações concorrentes acumulam em re-renders rápidos.
4. **components/ui/AnimatedBar.tsx — `backgroundColor` muda abruptamente** — Usar `interpolateColor` para transição suave em ≥95%.
5. **components/ui/AiLearning.tsx:75 — `Math.random()` não é determinístico** — Seedar por `userId + dia` para evitar flicker entre dois insights.
6. **app/(tabs)/index.tsx — streak "0 dias" aparece antes de userId carregar** — Ocultar streak enquanto `userId === undefined`.

### 🟡 Importantes

1. **supabase/functions/coach/index.ts — `GROQ_API_KEY!` sem validação no boot** — Retornar 503 se env não estiver setada.
2. **supabase/functions/coach/index.ts — `.single()` → `.maybeSingle()`** — PGRST116 quando daily_logs não existe pro dia.
3. **supabase/functions/coach/index.ts — `security_log` sem migration** — Confirmar tabela existe; sem ela a auditoria é silenciosa.
4. **components/ui/AiLearning.tsx — Query sem `.limit(50)`** — Dados de saúde desnecessários trafegando (LGPD).
5. **hooks/useStreak.ts — Query sem `.limit()`** — Até 900 rows em usuário ativo. Adicionar `.limit(500)`.
6. **hooks/useStreak.ts + AiLearning.tsx — `toLocaleDateString('pt-BR')` inconsistente entre devices** — Usar `toISOString().slice(0, 10)` com timezone explícito.
7. **app/(tabs)/index.tsx — Score number sem animação de entrada** — Envolver em `FadeIn.duration(400)` para coesão com AnimatedBars.

### 🟢 Sugestões

1. Renomear `isToday` → `loggedToday` em StreakResult
2. Expandir pool de insights do AiLearning (5-7 templates)
3. AnimatedBar `height: 3` para acessibilidade WCAG 1.4.11
4. Groq: adicionar `temperature: 0.7, top_p: 0.9`
5. AnimatedBar: flag `delayOnMount` para não re-animar em updates
6. AiLearning: `accessibilityRole="text"` + `accessibilityLabel`

### ✅ Aprovado sem ressalvas

- Edge Function auth (JWT via `supabase.auth.getUser`)
- Anti-injection (8 padrões + fallback neutro)
- RLS por `user_id` em todas as queries
- Sanitização de input (`slice(0, 500)`)
- `Math.min(value / max, 1)` e `max > 0` guard no AnimatedBar
- Empty state anti-shame no MealTimeline e Insights
- Migração Groq preserva contrato da Edge Function
- APIs Reanimated v4 e NativeWind v4 corretas
